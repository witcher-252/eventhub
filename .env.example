# Entorno de ejecución: "dev" o "prod"
DJANGO_ENV=prod

# Clave secreta para producción (reemplazar por una generada segura)
SECRET_KEY=tu_clave_secreta_muy_larga_y_segura_aqui

# Hosts permitidos separados por coma (en producción poner dominio o IP)
ALLOWED_HOSTS=localhost,127.0.0.1

# Puerto (opcional, por defecto 8000)
PORT=8000

# Debug mode (solo para desarrollo)
DEBUG=False

